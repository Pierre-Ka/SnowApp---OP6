{% extends 'layouts/main_frame.html.twig' %}

{% block content %}
    <h1 class="pb-3">Modifier cette image </h1>
    <div style="margin : 30px 0; padding-left: 30%">
        <img class="mx-auto" height="200" src=" {{ asset('uploads/pictures/'~picture.path ) }}" alt=""/>
    </div>

    {{ include('partials/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}


