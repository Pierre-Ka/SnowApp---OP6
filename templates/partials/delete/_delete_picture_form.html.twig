<div class="d-inline-block">
    <form method="post" class="d-inline-block" action="{{ path('app_picture_delete', {'id': picture.id}) }}" onsubmit="return confirm('Supprimer cette image ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
        <button class="bg-transparent border-none">
            <img src="{{ asset('assets/icon/delete.png') }}" class="width-edit-icon" alt=""> </button>
    </form>
</div>

