{% extends 'layouts/main_frame.html.twig' %}

{% block content %}
    <h1 class="pb-3">Modifier votre profil "{{ user.fullName }}" </h1>
    {{ form_start(form)  }}
            {{ form_row(form.firstName) }}
            {{ form_row(form.lastName) }}
    <div class="btn btn-secondary rounded mt-3 mb-5 p-2 shadow">
        <a href="#" style="color:white">Modifier mon mot de passe </a>
    </div>
    <button type="submit" class="btn btn-primary btn-lg w-100" formnovalidate >Modifier</button>
    {{ form_end(form)}}

{% endblock %}